import 'package:flutter/material.dart';

class WificardView extends StatelessWidget {
  const WificardView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}