import 'package:flutter/material.dart';

class InstgramcardView extends StatelessWidget {
  const InstgramcardView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}